/*
Is responsible for prompting the user for a string
*/

#ifndef GET_STRING
#define GET_STRING

char *get_string(char *message); // function that implements scanf to prompt the user for a string

#endif